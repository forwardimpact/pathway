// Public entry point for @forwardimpact/basecamp.
// Basecamp is primarily a CLI — this file exists so the package conforms
// to the repo-wide layout contract (spec 390). The runtime CLI dispatch
// lives in src/basecamp.js.
export * from "./basecamp.js";
