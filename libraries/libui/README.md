# libui

Web UI framework: rendering, routing, components, and design system.

## Getting Started

```js
import { createElement, div, h1, render, createRouter } from '@forwardimpact/libui';
import { createReactive, bind } from '@forwardimpact/libui';
```
