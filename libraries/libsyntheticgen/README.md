# libsyntheticgen

DSL parsing and deterministic entity generation for synthetic data.

## Getting Started

```js
import { createDslParser, createEntityGenerator } from '@forwardimpact/libsyntheticgen';
```
