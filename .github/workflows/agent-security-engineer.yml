name: "Agent: Security Engineer"

on:
  schedule:
    # Patch and audit after staff implements. Paris (CEST) times below.
    - cron: "53 2 * * *" # Night shift — 04:53 Paris, finishes before 07:00
  workflow_dispatch:
    inputs:
      task-amend:
        description: "Additional text appended to the task prompt for steering"
        required: false
        type: string

permissions:
  contents: write

jobs:
  kata:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/kata-action-agent
        with:
          app-id: ${{ secrets.KATA_APP_ID }}
          app-private-key: ${{ secrets.KATA_APP_PRIVATE_KEY }}
          anthropic-api-key: ${{ secrets.ANTHROPIC_API_KEY }}
          agent-profile: "security-engineer"
          task-text: >-
            Assess the current state of your domain and act on the highest-priority finding.
          task-amend: ${{ inputs.task-amend }}
