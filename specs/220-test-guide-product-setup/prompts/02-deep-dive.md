# Step 2: Deep-dive into Guide architecture and CLI

Continue your research into the Guide product. You need to understand its
architecture and CLI before installing.

Fetch and read these pages from www.forwardimpact.team:

1. The Guide internals page at https://www.forwardimpact.team/docs/internals/guide/
2. The CLI reference page at https://www.forwardimpact.team/docs/reference/cli/
3. The operations reference at https://www.forwardimpact.team/docs/internals/operations/

After reading, write a summary of:
- The Guide service stack (what services are needed and their ports)
- The agent orchestration pipeline (planner, researcher, editor)
- The knowledge pipeline (how data flows from HTML to graph + vector stores)
- The key CLI commands for managing Guide (environment, data, services)
- The configuration files that need to be set up

Save your summary to ./notes/02-architecture-summary.md
